// Task 4: Use array destrcturing to extract the frist and second elements from an array of numbers and them to the console.

let numbers = [1, 2, 3, 4, 5];
let [first, second] = numbers;
console.log(first, second);